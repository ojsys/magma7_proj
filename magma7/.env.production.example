# ============================================
# PRODUCTION ENVIRONMENT CONFIGURATION
# ============================================
# Copy this file to .env and update with your actual production values
# DO NOT commit the actual .env file to version control!

# ============================================
# DJANGO CORE SETTINGS
# ============================================

# CRITICAL: Generate a new secret key for production!
# You can generate one using: python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
DJANGO_SECRET_KEY=your-production-secret-key-make-it-long-random-and-secret

# CRITICAL: Set to 0 for production!
DJANGO_DEBUG=0

# CRITICAL: Set to your actual domain(s), comma-separated
DJANGO_ALLOWED_HOSTS=yourdomain.com,www.yourdomain.com

# ============================================
# DATABASE CONFIGURATION (MySQL/MariaDB)
# ============================================

# Database name (created in cPanel)
DB_NAME=username_magma7

# Database user (created in cPanel)
DB_USER=username_magma7user

# Database password (set in cPanel)
DB_PASSWORD=your-secure-database-password

# Database host (usually localhost for cPanel)
DB_HOST=localhost

# Database port (usually 3306 for MySQL)
DB_PORT=3306

# ============================================
# EMAIL CONFIGURATION
# ============================================

# Email backend for production
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend

# SMTP server (e.g., smtp.gmail.com, smtp.office365.com, mail.yourdomain.com)
EMAIL_HOST=smtp.gmail.com

# SMTP port (587 for TLS, 465 for SSL)
EMAIL_PORT=587

# Use TLS (1 for yes, 0 for no)
EMAIL_USE_TLS=1

# SMTP username (usually your email address)
EMAIL_HOST_USER=your-email@gmail.com

# SMTP password or app password
# For Gmail, create an App Password: https://myaccount.google.com/apppasswords
EMAIL_HOST_PASSWORD=your-app-password

# Default from email address
DEFAULT_FROM_EMAIL=noreply@yourdomain.com

# ============================================
# PAYMENT CONFIGURATION
# ============================================

# Enable/disable payments (1 = enabled, 0 = disabled)
PAYMENTS_ENABLED=1

# Payment provider: 'paystack' or 'stripe'
PAYMENT_PROVIDER=paystack

# Your website URL (with https://)
SITE_URL=https://yourdomain.com

# Currency code (NGN, USD, GHS, KES, ZAR, etc.)
CURRENCY=NGN

# ============================================
# PAYSTACK CONFIGURATION
# ============================================

# Paystack LIVE Public Key (starts with pk_live_)
# Get from: https://dashboard.paystack.com/#/settings/developer
PAYSTACK_PUBLIC_KEY=pk_live_your_live_public_key_here

# Paystack LIVE Secret Key (starts with sk_live_)
# CRITICAL: Keep this secret! Never share or commit to git!
PAYSTACK_SECRET_KEY=sk_live_your_live_secret_key_here

# ============================================
# STRIPE CONFIGURATION (Optional)
# ============================================

# Stripe LIVE Publishable Key (starts with pk_live_)
STRIPE_PUBLISHABLE_KEY=pk_live_your_publishable_key_here

# Stripe LIVE Secret Key (starts with sk_live_)
STRIPE_SECRET_KEY=sk_live_your_secret_key_here

# Stripe Webhook Secret (starts with whsec_)
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret_here

# ============================================
# SECURITY SETTINGS
# ============================================

# Force HTTPS redirect (1 = enabled, 0 = disabled)
# Enable this ONLY after SSL is properly configured
SECURE_SSL_REDIRECT=1

# ============================================
# NOTES
# ============================================

# 1. After creating this .env file, secure it:
#    chmod 600 .env

# 2. Never commit .env to version control
#    It should be in .gitignore

# 3. Keep backups of your .env file in a secure location

# 4. For production, always use:
#    - DJANGO_DEBUG=0
#    - Strong SECRET_KEY
#    - LIVE payment keys (not test keys)
#    - Actual domain in ALLOWED_HOSTS
#    - SSL enabled (HTTPS)

# 5. Test all configurations before going live!
